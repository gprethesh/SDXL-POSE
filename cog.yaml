build:
  gpu: true
  python_version: "3.10"
  system_packages:
    - "libgl1-mesa-glx"
    - "libglib2.0-0"
  python_packages:
    - "torch==2.0.1"
    - "diffusers>=0.20.0"
    - "transformers"
    - "accelerate"
    - "safetensors"

predict: "predict.py:Predictor"
